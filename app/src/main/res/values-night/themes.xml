<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TapGM" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/bluePrimary</item>
        <item name="colorPrimaryVariant">@color/bluePrimary</item>
        <item name="colorOnPrimary">@color/bluePrimary</item>
        <item name="colorControlNormal">@color/darkSty</item>
        <item name="colorAccent">@color/bluePrimary</item>
        <item name="color">@color/dark_semi_boldText</item>
        <item name="colorScheme">@color/dark_medText</item>
        <item name="colorError">@color/dark_regularText</item>
        <item name="colorBackgroundFloating">@color/darkMainBg</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/blueSec</item>
        <item name="colorSecondaryVariant">@color/blueSec</item>
        <item name="colorOnSecondary">@color/pinkSec</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/darkMainBg</item>
        <!-- Customize your theme here. -->
        <item name="android:windowContentTransitions">true</item>
        <item name="android:windowAnimationStyle">@style/transitionAnimation</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="android:fitsSystemWindows">true</item>
    </style>

    <style name="transitionAnimation" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/slide_in_right</item>
        <item name="android:activityOpenExitAnimation">@anim/slide_out_left</item>
        <item name="android:activityCloseEnterAnimation">@anim/slide_in_left</item>
        <item name="android:activityCloseExitAnimation">@anim/slide_out_right</item>
    </style>
</resources>